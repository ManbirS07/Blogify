<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include('./partials/nav.ejs') %>
    <div class="text-center">
        <h2 class="text-decoration-underline">
            <%= blog.title %>
        </h2>
        <% if (userr.name) { %>
            <h4>By ~ <%=userr.name%></h4>
        <% } %>
        
        <img src="<%=blog.coverImageUrl%>" width="500px">
        <div class="card mt-5 mx-auto overflow-auto" style="width: 1200px;">
            <div class="card-body">
                <pre class="mt-3" style="padding:5px"><%=blog.body%></pre>
            </div>
          </div>
    </div>

    <div class="mt-3">
        <h1 class="m-3">Comments (<%=(comments.length)%>)</h1>
        <% if (locals.user) { %>
        <form action="/blog/comment/<%= blog._id %>" method="post">
          <div class="m-3">
            <input
              type="text"
              name="content"
              class="form-control"
              placeholder="Enter your comment"
            />
            <button class="btn btn-sm btn-primary m-2" type="submit">Add Comment</button>
          </div>
        </form>
  
        <% } %>
        <div class="card col m-4 text-white bg-dark mb-3" style="width: 70rem">
        <div class="m-3">
            <% (comments)?.forEach(comment => { %>
                <h6 class="text-decoration-bold"><%= userr.name %></h6>
                <pre><%= comment.content %></pre>
                <% }) %>
        </div>
        </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>


